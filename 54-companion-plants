public Boolean companionPlants(String plant1, String plant2) {
    // Convert plant names to lowercase for case-insensitive comparison
        plant1 = plant1.toLowerCase();
        plant2 = plant2.toLowerCase();

        // Define the companion plant pairs
        Set<String> companionPairs = new Set<String>{'lettuce-cucumbers', 'lettuce-onions', 'onions-carrots', 'onions-tomatoes'};

        // Check if the given pair is in the set of companion pairs
        return companionPairs.contains(plant1 + '-' + plant2) || companionPairs.contains(plant2 + '-' + plant1);
}